# Renamed to ../build-server/server-port.sh — shim so older checkouts/paths don't break; delete once downstream checkouts bump.
source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/../build-server/server-port.sh"
